name: CI/CD Flow

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven
<<<<<<< HEAD

    - name: Show Java and Maven versions
      run: |
        java -version
        mvn -v

    - name: Build with Maven
      run: mvn -B clean package

    - name: List target directory
      run: ls -la target || echo 'no target dir'

    - name: Set up QEMU
      uses: docker/setup-qemu-action@v2

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Login to Docker Hub
      uses: docker/login-action@v2
      with:
=======

    - name: Show Java and Maven versions
      run: |
        java -version
        mvn -v

    - name: Build with Maven
      run: mvn -B clean install

    - name: List target directory
      run: ls -la target || echo 'no target dir'

    - name: Show Docker images (debug)
      run: docker images || true

    - name: Build & push Docker image
      uses: mr-smithers-excellent/docker-build-push@v6
      with:
        image: ricardoalmanzar/api-springboot-image
        tags: latest
        registry: Docker.io
        dockerfile: Dockerfile
>>>>>>> e5cddf481401e77dd156be65f639c04e08626785
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Build and push Docker image
      uses: docker/build-push-action@v4
      with:
        context: .
        file: Dockerfile
        push: true
        tags: ricardoalmanzar/api-springboot-image:latest
